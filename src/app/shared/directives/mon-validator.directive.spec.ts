import { MonValidatorDirective } from './mon-validator.directive';

describe('MonValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MonValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
